# setup_ubuntu

cd ~
git clone https://github.com/leonelcuevas/setup_ubuntu.git
cd setup_ubuntu
chmod +x setup.sh
./setup.sh

# open vim and run
# :PluginUpdate
# :PluginInstall
# in host system mkdir ~/Downloads/shared
